<li>
  <%= link_to(url, class: "bw-nav-item #{'is-active' if current_page?(url)}") do %>
    <% if local_assigns[:icon].present? %>
      <svg width="16" height="16"><use xlink:href="#<%= icon %>" /></svg>
    <% end %>
    <span><%= label %></span>
  <% end %>
</li>
