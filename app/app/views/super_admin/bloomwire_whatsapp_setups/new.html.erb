<% content_for(:title) do %>
  New WhatsApp Setup Mapping
<% end %>
<% content_for(:breadcrumb) do %>
  <%= bw_crumbs('Bloomwire', ['WhatsApp Setups', super_admin_bloomwire_whatsapp_setups_path], 'New setup mapping') %>
<% end %>

<div class="bw-page-head">
  <div><h1 class="bw-page-title">New setup mapping</h1></div>
  <%= link_to 'Back to setups', super_admin_bloomwire_whatsapp_setups_path, class: 'bw-btn bw-btn--ghost' %>
</div>

<div class="bw-card bw-card--form">
  <%= render 'form', url: super_admin_bloomwire_whatsapp_setups_path, method: :post %>
</div>
